let nome = "Alan Duarte de Medeiros";

//let resultado = nome;

//let resultado = nome.replace('Alan', 'joao');

//let resultado = nome.toUpperCase();

//let resultado = nome.toLowerCase();

//let resultado = nome.concat(" concatenando");

//let resultado = nome.trim();

//let resultado = nome.charAt(7);

let resultado = nome.split(" ");

console.log("Resultado: ", resultado);

//nome.replace() = ele recebe 2 parâmetros, 1: a string que voce quer substituir e, 2: a string que voce quer colocar no lugar
//nome.toUpperCase() = ele vai transformar toda a sua string em maiúscula.
//nome.toLowerCase() = ele vai transformar toda a sua string em minuscula.
//nome.concat() = ele vai pegar o valor da string e vai concatenar com o valor que voce passar (MAIS INÚTIL IMPOSSÍVEL!!)
//nome.trim() = ele vai limpar todos os espaços em branco.
//nome.charAt() = ele vai mostrar que caractere esta no posição que voce quer
//nome.split() = ele transforma a sua string em um ARRAY